APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Local (exemple)
# DB_CONNECTION=pgsql
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_DATABASE=ompay
# DB_USERNAME=yatedene
# DB_PASSWORD=faye0000

# Railway / URL de connexion (exemple)
# Railway fournit une variable `DATABASE_URL` au format:
#   postgres://<user>:<password>@<host>:<port>/<database>?sslmode=require
# Si vous utilisez Railway, copiez-collez la `DATABASE_URL` fournie par Railway
# et laissez `DB_CONNECTION=pgsql` (Laravel supporte DATABASE_URL automatiquement).

DB_CONNECTION=pgsql
# Exemple (remplacez par la valeur fournie par Railway) :
# DATABASE_URL=postgres://postgres:XXXXXXXX@egress-us-west-2.railway.app:5432/railway
DATABASE_URL=

# Optionnel: paramètres DB découpés (si vous préférez les définir séparément)
DB_HOST=postgres.railway.internal
DB_PORT=5432
DB_DATABASE=railway
DB_USERNAME=postgres
DB_PASSWORD=

# Notes:
# - Sur Render, définissez la variable d'environnement `DATABASE_URL` (valeur fournie
#   par Railway) dans le tableau de configuration du service. Ne commitez jamais
#   de secrets dans le dépôt.
# - Après avoir configuré la base Railway et le `DATABASE_URL` sur Render, exécutez
#   les migrations sur l'environnement de production :
#     php artisan migrate --force
# - Assurez-vous d'avoir défini `APP_KEY` (générez-la localement avec `php artisan key:generate`
#   puis collez-la dans l'interface Render comme `APP_KEY`).


BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Twilio Configuration
TWILIO_SID=
TWILIO_TOKEN=11918b17ceabced0c66b9fbfa576cb8f
TWILIO_FROM=+14482305044

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
